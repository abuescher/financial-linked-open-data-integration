Due to very long times when performing GETs on both sources DBpedia and SEC I suggest GETting a datastore first (it can be used for ALL other queries also) and executing queries afterwards
-> call.txt Version (1)

Still one can perform both within one call!
-> call.txt version (2)